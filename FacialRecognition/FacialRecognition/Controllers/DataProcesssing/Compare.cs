using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using FacialRecognition.Models;

namespace FacialRecognition.BL
{
    public class Compare
    {
        /*
        private PersonModel person;
        private PersonModel persons;

        public Compare(PersonModel person, PersonModel persons)
        {
            this.person = person;
            this.persons = persons;
        }

        public bool NameEqual()
        {
            if(person.Name.Equals(persons.Name))
                return true;
            return false;
        }
        public bool AgeEqual()
        {
            if(person.Age.Equals(persons.Age))
                return true;
            return false;
        }
        public bool HairEqual()
        {
            if(person.Face.Hair.Color.Equals(persons.Face.Hair.Color) && person.Face.Hair.Size.Equals(persons.Face.Hair.Size))
                return true;
            return false;
        }
        public bool MouthEqual()
        {
            if(person.Face.Mouth.Color.Equals(persons.Face.Mouth.Color) && person.Face.Mouth.Shape.Equals(persons.Face.Mouth.Shape))
                return true;
            return false;
        }
        public bool NoseEqual()
        {
            if(person.Face.Nose.Shape.Equals(persons.Face.Nose.Shape))
                return true;
            return false;
        }
        public bool ShapeEqual()
        {
            if(person.Face.Shape.Equals(persons.Face.Shape))
                return true;
            return false;
        }
        public bool EarsEqual()
        {
            if(person.Face.Ear.ElementAt(0).perfored.Equals(persons.Face.Ear.ElementAt(0).perfored) && person.Face.Ear.ElementAt(1).perfored.Equals(persons.Face.Ear.ElementAt(1).perfored) && person.Face.Ear.ElementAt(0).Shape.Equals(persons.Face.Ear.ElementAt(0).Shape) && person.Face.Ear.ElementAt(1).Shape.Equals(persons.Face.Ear.ElementAt(1).Shape))
                return true;
            return false;
        }
        public bool EyesEqual()
        {
            if(person.Face.Eye.ElementAt(0).Color.Equals(persons.Face.Eye.ElementAt(0).Color) && person.Face.Eye.ElementAt(1).Color.Equals(persons.Face.Eye.ElementAt(1).Color) && person.Face.Eye.ElementAt(0).Type.Equals(persons.Face.Eye.ElementAt(0).Type) && person.Face.Eye.ElementAt(1).Type.Equals(persons.Face.Eye.ElementAt(1).Type))
                return true;
            return false;
        }
         */ 
    }
}